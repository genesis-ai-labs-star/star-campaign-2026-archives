# 2026-03-02

## 系统与模型状态
- 系统深度体检了一轮 OpenClaw 环境和模型通路，明确：
  - OpenAI / GPT-5.1 API key 有效，当前主会话即跑在 gpt-5.1。
  - Anthropic / Claude Sonnet 4.6 云端 API key 有效，已在 cron 任务中实战使用，有稳定 token 与缓存记录。
  - Grok 模型曾经出现 Unknown model 报错，新版本已识别 `grok-4-1-fast-reasoning`，slash 会话在用。
  - Gemini 对话模型调用可用，但 web_search 使用的 Gemini Generative Language API key 已过期，所有 web_search 调用 400 报错。
  - 当前没有启用本地大模型后端（Ollama/vllm 等），所有推理走云端。

## 架构图与项目
- 基于微信文章重新渲染了 OpenClaw 专业架构图（Graphviz），文件 `openclaw_architecture_pro.png`，并通过 Telegram 发送。
- 对 deepdive-engine 读取最新 STRUCTURE.md 后，设计并绘制了 Genesis.ai / DeepDive 的高层专业架构图：
  - DOT: `deepdive_arch.dot`
  - PNG: `deepdive_arch.png`
  - 通过 Telegram 发给用户。

## 工作方式进化
- 用户明确提出希望“现场测试而不是靠猜测”，要求所有模型与系统诊断基于真实命令输出和 API 调用结果。
- 建立了一套自我约束的“星宝进化协议 v0.1”：
  - 审计/状态类问题：先查 MEMORY 和历史总结，再跑 `openclaw status` / 日志 / 实际 API 调用，最后结构化汇报（表格化：模型/状态/证据/建议）。
  - 规划/Roadmap 时，所有任务状态必须与 `MEMORY.md` 对齐，不能把已完成事项重新当成待办。
  - 工具使用优先考虑无需 web_search 的路径（本地文件、已有知识），web_search 报错时要直说 key 失效，不伪装有网。
  - 每次被用户指出“要进化”的地方，都要提炼成通用规则写入这套协议，并在后续同类场景中执行。

## 用户偏好加深
- 用户非常看重“事实 + 证据链 + 下一步动作”，不接受靠经验猜模型状态或任务状态。
- 对架构图有较高要求：需要专业、系统工程视角的图，而不是简单框图；希望能多轮迭代到与具体子系统名称一一对应。