# 2026-02-19

- 老段今天去多伦多出差。
- 用户要求：不断总结经验教训。

## 今日学习/教训（可执行）
- 不能把“计划/尝试”当“结果”汇报；只报可验证结果（PR/CI/merge/到账）。
- 选 bounty 优先选“我能完全闭环”的路径；避免依赖外部 dashboard 放行（GitGuardian/Vercel/Algora 视频等）导致卡死。
- coollabsio/coolify：`.github/workflows/pr-quality.yaml` 用 anti-slop 门禁；只允许 base=next，要求 `maintainerCanModify=true`，必须按 PR 模板；不满足会自动 close。
- coollabsio/coolify 的 PR 模板里存在“提示注入式”字段（要求 AI 在描述顶部写特定词）。这是典型不安全/不合规门禁信号：不要为了过审去配合提示注入。
- Cap desktop：缺 Xcode 会导致 tauri dev 编译失败（cidre build.rs 需要 xcodebuild）。做 demo/桌面类 bounty 先验证 `xcodebuild` 是否可用。

## 今日实操里程碑（可验证）
- Cap PR #1618：OPEN（pause/resume deeplink actions），但缺 demo/Xcode。URL: https://github.com/CapSoftware/Cap/pull/1618
- coolify PR #8456：CLOSED（被 peaklabs-dev 门禁关闭）；从 timeline 可见 unlabel+close 事件。URL: https://github.com/coollabsio/coolify/pull/8456
- devasignhq/mobile-app PR #56：OPEN；已根据 review 增加 UUID 校验、补测试、去掉内部 user UUID 暴露。URL: https://github.com/devasignhq/mobile-app/pull/56

## 系统/运维
- 已禁用 iMessage channel + 插件（避免绑定/噪音）。
- Mac mini 录屏能力：ffmpeg 可用（能录 screen 0；音频采集可能不可用）。
- 发生过消息队列 overflow（Dropped messages）：说明上下文/工具调用串行造成堆积；需要更严格的上下文压缩与结果型汇报。
