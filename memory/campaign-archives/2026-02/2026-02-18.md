# 2026-02-18

## Learning summary (daily)
- WhatsApp/iMessage 编码偶发乱码：尽量避免 emoji/特殊符号；必要时改用纯文本。
- WhatsApp 回复延迟更多来自模型生成/工具串行调用，而非本地网络；关闭 thinking、换更快模型能改善。
- Ollama + 本地 DeepSeek-R1:7b 在 M4 上可用，已作为 OpenClaw fallback 模型（`ollama/deepseek-r1:7b`）。
- Genesis AI MCP Server 可用：真实 MCP 端点是 `https://genesis-ai-backend.up.railway.app/api/v1/mcp`（带 API Key），工具列表可通过 JSON-RPC `tools/list` 获取。
- `genesis_deep_research` 多次在报告合成阶段触发 120s 超时；需要拆分任务/降深度或改用分段搜索+本地汇总。
- 赚钱目标推进时必须“可验证进展”：所有 bounty/PR/账号切换等结论必须附可复核证据；第三方搜索只当线索。
- Coding agent 运行不稳定时要以 `process list` / sessionId 为准；Claude Code 可能因 LSP/MCP 报错导致分析失败。
- 已落实流程约束：每 6 小时主动汇报进展（cron 已配置）；每日晚间必须总结当天学习。

## Notes
- 配置：主模型切到 Gemini（flash/3 flash preview 期间有多次切换）；thinking 已关闭。
- MCP：已安装 mcporter，并写入 `~/.openclaw/config/mcporter.json`（server 名 `genesis`）。
- GitHub：已通过 PAT 登录品牌账号 `genesis-ai-labs-star`；并验证 `OpenClaw-Star-Key` SSH key 已存在于该账号 keys 列表（需要 `admin:public_key` scope 才能自动管理）。
- 用户明确要求：禁止任何编造/推测式肯定回复；汇报需分为已验证/进行中/计划。
